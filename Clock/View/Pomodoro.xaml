<UserControl x:Class="Clock.View.Pomodoro"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Clock.ViewModel"
             xmlns:local="clr-namespace:Clock.View"
             mc:Ignorable="d" 
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:PomodoroVM />
    </UserControl.DataContext>

    <Grid>
        <StackPanel>
            <TextBlock Height="300" 
                       Width="400" 
                       TextWrapping="Wrap"
                       FontSize="34"
                       Background="AliceBlue"
                       Text="kích thước view của các mục (pomo, alarm,...) ở trong phần page.xaml, mục nào size cũng như nhau"/>
        </StackPanel>
    </Grid>
</UserControl>
